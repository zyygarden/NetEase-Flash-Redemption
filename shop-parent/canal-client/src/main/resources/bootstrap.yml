spring:
  application:
    name: canal-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.72.159:8848 #nacos中心地址
        file-extension: yaml # 配置文件格式
        shared-configs:
          - data-id: nacos-discovery-config-dev.yaml
          - data-id: redis-config-dev.yaml
  profiles:
    active: dev # 环境标识

